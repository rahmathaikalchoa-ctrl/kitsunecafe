// No custom JS: this is a server-rendered Livewire app — Livewire, Alpine, and
// Flux's scripts are injected by @fluxScripts in the layouts. This file stays as
// the Vite entry referenced by @vite([... 'resources/js/app.js']).
